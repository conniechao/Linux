CTRL+ALT+T进入终端界面

进入root用户命令：
1、su
2、sudo su
3、su root

root切换到普通用户命令：
1、su username
2、exit
3、Ctrl+D

root用户设定密码： 
sudo passwd root

禁用root登录:sudo passwd -l root(root密码还保存着，su root认证失败，sudo su可进入root)
启用root登录:sudo passwd -u root
